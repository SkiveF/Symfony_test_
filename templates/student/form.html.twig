{% extends 'base.html.twig' %}
{% block body %}
    <h1>{{ form_title }}</h1>
        <div class="container">
            <div class="row justify-content-center ">
                <div class="col-md-6">
                    <div class="form-group">
                        <p class="form-controle">{{ form(form_student) }}</p>
                    </div>    
                </div> 
            </div>  
        </div>         
{% endblock %}