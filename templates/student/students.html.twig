{% extends 'base.html.twig' %}

{% block title %}Hello ProductController!{% endblock %}

{% block body %}

<div class="container-xl">
	<div class="table-responsive">
		<div class="table-wrapper">
			<div class="table-title">
				<div class="row">
					<div class="col-sm-6">
						<h1>Liste des Students</h1>
					</div>
					<div class="col-sm-6">
                        <a href="{{ path('add_student') }}" class="btn btn-success"><i class="material-icons">&#xE147;</i>Ajouter un Students</a>
					</div>
				</div>
			</div>
                <table class="table table-striped table-hover">
                    <thead  class="thead-dark">
                        <th>Firstname</th>
                        <th>Lastname</th>
                        <th>Numéro</th>
                        <th>Actions</th>
                        
                    </thead>

                    <tbody>
                    {% for student in students %}
                    <tr>
                        <td>{{ student.Firstname }}</td>
                        <td>{{ student.Lastname }}</td>
                        <td>{{ student.Numetud }}</td>
                        <td>
                            <a class="edit" href="{{ path('update_student', {'id': student.id}) }}"><i class="material-icons" data-toggle="tooltip" title="Edit">&#xE254;</i></a>
                            <a class="delete" href="{{ path('delete_student', {'id': student.id}) }}"><i class="material-icons" data-toggle="tooltip" title="Delete">&#xE872;</i></a>
                        </td>
                    </tr>
                    {% endfor %}
                    </tbody>
                </table>
        </div>
	</div>        
</div>

{% endblock %}